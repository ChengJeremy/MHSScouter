package com.example.jerem.mhsscouter;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class MHSScouter extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_mhsscouter);
    }

    protected void clicked(){
        //change class to an input class with database and relearn all that
        //allow to take images of robots if necessary???
        //ability to link with online web database - consult with Abhinav
        //offline mode???
        //ability to edit rows and columns also according to online database... make everything local for now though
        //change app logo widget design - it cannot be just an android robot :P
        //why does the window get messed up when I turn it horizontal? Shouldn't everything be relative to each other? Look into this later.
    }
}
